name: tfsec
on:  
  pull_request:

jobs:
  tfsec:
    name: tfsec
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@93ea575cb5d8a053eaa0ac8fa3b40d7e05a33cc8 # v3.1.0
      - uses: aquasecurity/tfsec-pr-commenter-action@f2c4d295e314be2de23d24231e19db528126aa6a # v1.3.0
        with:
          github_token: ${{ github.token }}
